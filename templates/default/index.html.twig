{% extends 'base.html.twig' %}

{% block title %}

    hello page

{% endblock %}

{% block body %}

    <ul>
        {% for category in categories %}
            <li><a href="{{ category.id }}">{{ category.name }}</a></li>
        {% endfor %}
    </ul>

{% endblock %}
